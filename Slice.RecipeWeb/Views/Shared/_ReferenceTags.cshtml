@using Slice.Web.Models
@using Slice.Data
@using Slice.RecipeWeb.Controllers
@model ReferenceKeywordsViewModel

<p class="keywords">
  <strong>Tags: </strong>
  @if (Model.KeywordList.Count > 0)
  {
    for (int index = 0; index < Model.KeywordList.Count - 1; index++)
    {
      ReferenceKeywordInfoDto item = Model.KeywordList[index];
    <span>@Html.ActionLink(item.KeywordName, RecipeController.TagAction, RecipeController.ControllerName, new { tag = item.KeywordSlug }, new { }),</span>
    }
    ReferenceKeywordInfoDto lastItem = Model.KeywordList[Model.KeywordList.Count - 1];
    <span>@Html.ActionLink(lastItem.KeywordName, RecipeController.TagAction, RecipeController.ControllerName, new { tag = lastItem.KeywordSlug }, new { })</span>
  }
</p>
