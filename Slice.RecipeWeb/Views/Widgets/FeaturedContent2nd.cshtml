@using Slice.Web.Models.Widgets
@model FeaturedContent2nd

@if (Model.HasValue)
{
  <div id="featuredcontent2" class="featuredcontent">
    <h3>@Model.Title</h3>
    @Html.Partial("_CardView", Model.CardViewViewModel)
  </div>
}